<% klass = table_name.split("_").map{|e| e.capitalize}.join %>
class Add<%= klass %> < ActiveRecord::Migration
  
  def up
    create_table(:<%= table_name %>) do |t|
    <% attributes.each_with_index do |a, i| %>
      t.string '<%= a %>', :size => <%= @sum_attr_size[i] %>
    <% end %>
    end
  end      

  def down
    drop_table(:<%= table_name %>)
  end 
end